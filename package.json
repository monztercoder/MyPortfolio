{
  "name": "portfolio",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "sass-dev": "sass --watch --update --style=expanded scss:css",
    "start": "concurrently \"npm run dev \" \"npm run sass-dev\""
  },
  "devDependencies": {
    "sass": "^1.51.0",
    "vite": "^2.6.4"
  },
  "dependencies": {
    "concurrently": "^7.2.1",
    "three": "^0.134.0"
  }
}
